import Ember from 'ember';

export const {
  ViewUtils: {
    getChildViews,
    getRootViews,
    getViewBoundingClientRect,
    getViewBounds,
    getViewClientRects,
    getViewElement,
    isSimpleClick
  }
} = Ember;
